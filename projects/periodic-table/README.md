# 元素周期表

Vite + Vue 3 + TypeScript + Tailwind CSS 单文件 HTML 项目。

## Git 约定

本项目相关改动统一在分支 **`feat/periodic-table`** 上进行（开发、提交、合并均以此分支为准）。

## 开发

```bash
npm install
npm run dev
```

## 构建（输出单 HTML）

```bash
npm run build
```

构建产物在 `dist/index.html`，JS/CSS 已内联。
